<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_PreBuild_Order__c</fullName>
    <defaultValue>false</defaultValue>
    <description>Indicates a link from opportunity to the PBO to eliminate duplicate production signals (ignore the open opp demand because it is already in the demand plan via the ERP order).</description>
    <externalId>false</externalId>
    <inlineHelpText>Check if already in the Demand Plan via an ERP order</inlineHelpText>
    <label>Pre Build Order</label>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
