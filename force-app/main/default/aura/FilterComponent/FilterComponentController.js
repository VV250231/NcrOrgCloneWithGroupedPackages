({
	toogleSelect : function(cmp, event, helper) {
        //cmp.set('v.isSelected', !cmp.get('v.isSelected'));
        
        /*var myEvent = cmp.getEvent("myComponentEvent");
        myEvent.setParams({"param": cmp.get("v.FilterName"),
                           "IsSelected" : cmp.get("v.answered")});
        myEvent.fire();*/
	}
})