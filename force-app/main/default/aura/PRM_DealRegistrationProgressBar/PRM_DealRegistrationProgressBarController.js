({  
    
    //Launched at component initialization.  
    doInit  : function(component, event, helper) {  
        //Call helper to create/Update the Porgression indicator bar 
        helper.initPath(component, event, helper);  
    },  
})